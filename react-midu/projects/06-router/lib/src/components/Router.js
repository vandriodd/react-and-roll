import{jsx as _jsx}from"react/jsx-runtime";import{EVENTS}from"../utils/consts";import{getCurrentPath}from"../utils/getCurrentPath";import{useEffect,useState,Children}from"react";import{match}from"path-to-regexp";const Router=({routes=[],defaultComponent:DefaultComponent=()=>null,children})=>{const[currentPath,setCurrentPath]=useState(getCurrentPath());useEffect(()=>{const onNavigate=()=>{setCurrentPath(getCurrentPath())};window.addEventListener(EVENTS.PUSHSTATE,onNavigate);window.addEventListener(EVENTS.POPSTATE,onNavigate);return()=>{window.removeEventListener(EVENTS.PUSHSTATE,onNavigate);window.removeEventListener(EVENTS.POPSTATE,onNavigate)}},[]);let routeParams={};const routesFromChildren=Children.map(children,({props,type})=>{const{name}=type;const isRoute=name==="Route";return isRoute?props:null});const routesToUse=routes.concat(routesFromChildren).filter(Boolean);const Page=routesToUse.find(({path})=>{if(path===currentPath)return true;const matcherUrl=match(path,{decode:decodeURIComponent});const matched=matcherUrl(currentPath);if(!matched)return false;routeParams=matched.params;return true})?.component;return Page?_jsx(Page,{routeParams:routeParams}):_jsx(DefaultComponent,{routeParams:routeParams})};export default Router;