# Error Boundary Component

This project contains a simple React Error Boundary component that can be used to catch JavaScript errors anywhere in the child component tree, log those errors, and display a fallback UI instead of the component tree that crashed.
